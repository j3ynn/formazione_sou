apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "exbonus.fullname" . }}-test-connection"
  labels:
    {{- include "exbonus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "exbonus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
